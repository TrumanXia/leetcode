//package leetcode.array;
//
//import java.util.*;
//import java.util.stream.Collectors;
//
//public class Main {
//    public static void main(String[] args) {
//        List<String> lsit = new ArrayList<>();
//        List<String[]> collect = lsit.stream().map(word -> word.split("")).collect(Collectors.toList());
////        List<String[]> collect = lsit.stream().map(word -> word.split("")).flatMap(item -> item)
////        "ss".replace("".charAt(i), )
//        Set<String> set = new HashSet<>();
//        Map<Character, Boolean> baseCharsMemo = new HashSet<Character, Boolean>();
//            "ss".replace()
//    }
//}
